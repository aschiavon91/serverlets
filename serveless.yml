service: Servelets

provider:
  name: aws
  runtime: nodejs10
  stage: dev

  function:
    hello: 
      handler: dist/hello.handler
      events:
        - http:
          path: hello
          method: GET
          cors: true